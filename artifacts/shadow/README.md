# Shadow artifact bundle

This directory is reserved for the frozen configuration hash, hypothetical
decision ledger, availability audit and replay reconciliation. It intentionally
contains no fabricated observation window. Populate it only from the
read-only runner after the configuration is frozen.
