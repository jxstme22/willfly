# Neural experiment v0.3 review

The sparse graph, frozen reservoir, ordinary readout, controls and ablation
machinery are implemented. The current connectome manifest is
`pending_verification`; no MaleCNS release, license or biological fidelity is
claimed yet.

The progression decision remains `INCONCLUSIVE` until the same causal dataset,
policy, ledger, cost assumptions, at least five seeds, four chronological test
windows and paired uncertainty gates are available. A surviving wrapper or
feature effect must not be attributed to biological topology.
