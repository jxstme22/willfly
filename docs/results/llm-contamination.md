# Historical text/LLM contamination review

Text claims require publication time, retrieval time, source URL and source
span. Claims published after a feature cutoff are excluded. LLM extraction is
schema-checked, budgeted and optional; invalid output, hostile text, timeout or
budget exhaustion falls back to an unavailable numerical feature. LLM output
never creates a market outcome label or changes permissions.

Current status: `INCONCLUSIVE` until a permitted, time-stamped calibration set
and source access are available.
